# Minimal dependency set to run the VisionPilot inference demos
# (see requirements.txt for the full training environment).
# Installed with --break-system-packages: pip's /usr/local copies shadow any
# Debian-owned versions (e.g. python3-numpy pulled in by websockify).
numpy>=1.21.0,<2.0.0
opencv-python>=4.10.0.84
Pillow>=11.3.0
torch>=2.7.0
torchvision>=0.22.0
cmapy>=0.6.6
tqdm>=4.66.0
